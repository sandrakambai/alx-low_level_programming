#include <stdio.h>
/**
 * main - starting function
 *
 * Description: Prints hexademical numbers
 *
 * Return: 0
 */
int main(void)
{
	int f;
	char let;

	for (f = '0'; f <= '9'; f++)
	putchar(let);
	putchar('\n');
	return (0);
}
